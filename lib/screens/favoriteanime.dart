import 'package:flutter/material.dart';

class FavoriteAnime extends StatelessWidget {
  const FavoriteAnime({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
    );
  }
}